---
name: ecommerce-white-background-image
description: 根据 1 张电商参考图与 1–5 张商品实拍细节图，生成 3 个 800×800、1:1、纯白背景且带自然接触阴影的高保真电商商品图。用户要求参考样图的机位、构图、布光、阴影或陈列方式，同时严格保留实拍商品的造型、颜色、材质、Logo、标签、包装文字和其他真实细节时使用。
---

# 电商白底商品图

把商品实拍图视为商品事实来源，把参考图仅视为拍摄风格来源。优先编辑最合适的实拍主图，不要从参考图复制或臆造商品。

## 输入要求

确认输入包含：

- 1 张参考图；
- 1–5 张同一商品的实拍细节图。

根据用户说明、文件名和画面内容区分两类图片。角色不明确或实拍图疑似属于不同商品时，先询问用户；信息明确时直接生成，不重复确认。

如果图像工具无法同时读取全部输入，不要静默丢弃图片。要求用户重新附上缺失图片或减少数量。

## 约束优先级

按以下顺序处理冲突：

1. 商品实拍图中的真实细节；
2. 用户明确提出的商品要求；
3. 参考图的摄影表现；
4. 默认电商规范。

必须保留实拍商品的：

- 造型、结构、轮廓和部件数量；
- 尺寸关系和比例；
- 主色、辅色、透明度与表面材质；
- Logo、商标、标签、包装文字及其位置；
- 接缝、纹理、开孔、按钮、配件等可见细节。

只借鉴参考图的：

- 相机高度、俯仰、方向和透视；
- 主体大小、留白、居中方式和陈列关系；
- 光源方向、软硬程度、明暗层次和高光；
- 接触阴影的方向、范围、浓淡和柔度。

不要复制参考图中的品牌、文字、商品造型、装饰、配件或背景元素。不要凭常识补全实拍图中看不清的细节。

## 项链与吊坠构图

处理项链、吊坠等悬挂商品时，先从参考图记录吊坠中心的相对高度、上下留白、链条出画位置、悬垂曲线和吊坠朝向，再生成或编辑：

- 参考图若呈圆缓悬垂形态，使用连续、对称的圆弧 V 或浅悬链线；不要画成两条僵直线段或在吊坠处形成尖角。
- 用户要求链条从两个顶角出画时，使左右链条分别自然延伸至左上角和右上角，并保持链节尺寸与粗细一致。
- 把链条本身也视为商品事实，不要用通用首饰链替代。逐张核对实拍图中的链型、链节轮廓与长宽比、开口大小、线材粗细、相邻链节朝向、连接方式、金属颜色和表面高光；只从参考图借鉴链条的整体悬垂路线。
- 实拍图显示开口椭圆链时，明确提示链节为细圆线材、中心镂空、相邻链节交替方向穿接，并禁止误画成绳链、盒子链、珠链、密实扁链或连续金属丝。
- 用户要求吊坠完全正面展示时，消除俯仰、偏航、旋转和透视歪斜；让重复部件等宽、平行，上下边缘水平对齐，左右侧边可见程度对称。
- 不要默认把吊坠放在画面中心。以参考图和用户反馈为准检查垂直位置；底部空白明显多于构图需要时，将吊坠适度下移。正方形项链主图可先尝试把吊坠中心放在画布高度的 70%–75%，再按主体尺寸和参考图微调。
- 接触阴影跟随吊坠移动，保持轻、软、近，不要让阴影留在原位置。

## 生成流程

1. 检查全部输入图，建立商品细节清单和参考摄影清单。
2. 选择与参考机位最接近、清晰度最高的实拍图作为主图；把其余实拍图仅用于补充真实细节。
3. 优先对实拍主图进行背景、机位观感、布光和阴影编辑，尽量避免重新绘制商品本体。
4. 调用可用的图像生成或图像编辑工具，并向工具同时提供参考图和所有实拍图。
5. 在提示词中明确标注每张图的角色，使用以下核心约束：

   ```text
   以商品实拍图为唯一商品事实来源。严格保留商品的造型、比例、颜色、
   材质、Logo、标签、包装文字和所有可见细节。参考图只用于机位、构图、
   光线、自然接触阴影和陈列方式，不得复制参考图中的商品、品牌、文字、
   配件或装饰。输出单件商品电商主图，1:1，纯白背景 RGB(255,255,255)，
   边缘干净，无杂物、无额外文字、无水印。看不清的细节保持不可见，
   不得猜测或补造。
   ```

6. 生成 3 个版本。三个版本只能在取景距离、留白和阴影柔度上有轻微差异，不得改变商品本身。
7. 分别检查三个版本，不合格时针对具体问题重试，不要把明显失真的版本交付。
8. 确保最终文件为 800×800。生成工具不能直接输出该尺寸时，先生成高质量正方形图，再在 macOS 上运行 `scripts/export_800.sh` 导出。

## 迭代修改

用户基于已生成版本提出修改时：

1. 把用户认可的最新版本作为编辑目标，同时继续提供原参考图和全部商品实拍图。
2. 在提示词中逐项写明“只改变什么”和“保持什么不变”。构图修改不得顺带重绘商品结构、材质、颜色、部件数量、链节比例、布光或背景。
3. 尽量量化位置变化，例如“在 800×800 画布中下移约 40–70 像素”或“吊坠中心位于画布高度的 72%–74%”，避免只写“往下一点”。
4. 每轮只做用户当前要求的改动，并另存新版本，不覆盖上一轮成品。
5. 编辑后重新检查链条连续性、对称性、吊坠朝向、上下留白和阴影位置；不合格时针对单一问题重试。

## 质量检查

逐张确认：

- 画布为 800×800、1:1；
- 背景是均匀纯白色；
- 商品完整、居中合理，没有切边或畸变；
- 商品色彩、材质和部件与实拍图一致；
- Logo、标签和包装文字没有替换、乱码或虚构；
- 阴影只用于落地感，方向符合参考图且不过黑；
- 项链类商品的链条曲线、出画位置和吊坠高度符合参考图或用户要求，没有僵直尖锐的 V 形；
- 链型、链节形状、长宽比、开口、线径、交替朝向与实拍图一致，没有被替换成通用或错误链型；
- 要求正面展示的吊坠没有倾斜或透视歪斜，重复部件等宽、平行且对称；
- 上下留白均衡，吊坠没有因位置偏高而在底部留下过多无效空白；
- 没有参考图中的品牌、商品、道具、文字或水印；
- 三个版本存在可辨识但克制的摄影差异。

如果 Logo、标签或包装文字无法准确保留，明确指出问题并继续重试；仍无法解决时，不要声称结果完全准确。商品关键细节在所有实拍图中都不可辨认时，先请用户补充清晰照片。

## 交付

展示三个版本并标记为“版本 1”“版本 2”“版本 3”。说明它们在构图或阴影上的差异，避免声称看不清或无法验证的细节已经被准确还原。
